# Backend — copy this to backend/.env
OPENAI_API_KEY=sk-...your-openai-key-here...
PORT=5000
NODE_ENV=development

# Frontend — copy this to frontend/.env (optional, defaults to localhost)
NEXT_PUBLIC_API_URL=http://localhost:5000
